/* Aufgabenstellung

Bitte genau lesen!

Deklariere die Funktion intro(). Im Funktionskörper {} passiert nun folgendes:
Deklariere die Variable a mit dem Wert 1 + 3.
Gib zusätzlich Hello World in der Konsole aus.
Gib den String “1+3 = “ + a in der Konsole aus.
Denk daran, die Geschweifte Klammer } zu schließen!

Wichtig! Um das Ergebnis sehen zu können, müssen wir die Funktion aufrufen.
Wichtig! Vergiss nicht, die Hinweise auf der nächsten Folie zu lesen.
Wichtig! Nutze die Konsole im Browser, um das Ergebnis zu sehen.
 */

function intro() {
  let a = 1 + 3;
  console.log("Hello World");
  console.log("1+3 = " + a);
}

intro();
